/*
** Extension System - Public API
**
** Extension authors should include this header for public type
** definitions.  The full internal API (registry operations) is in
** src/extension.h and is used only within the library.
**
** The public entry points (lemon_extension_registry_init, etc.)
** are declared in parser.h.
*/
#ifndef EXTENSION_PUBLIC_H
#define EXTENSION_PUBLIC_H

/* The public extension API entry points are declared in parser.h.
** This header exists as a placeholder for future public extension
** types that do not duplicate the internal definitions. */

#endif /* EXTENSION_PUBLIC_H */
